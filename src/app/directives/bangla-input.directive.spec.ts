import { BanglaInputDirective } from './bangla-input.directive';

describe('BanglaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BanglaInputDirective();
    expect(directive).toBeTruthy();
  });
});
