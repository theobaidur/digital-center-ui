import { SwitchLanguagePropPipe } from './switch-language-prop.pipe';

describe('SwitchLanguagePropPipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchLanguagePropPipe();
    expect(pipe).toBeTruthy();
  });
});
