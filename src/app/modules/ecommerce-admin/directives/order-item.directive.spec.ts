import { OrderItemDirective } from './order-item.directive';

describe('OrderItemDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderItemDirective();
    expect(directive).toBeTruthy();
  });
});
