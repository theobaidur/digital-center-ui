import { EarningItemDirective } from './earning-item.directive';

describe('EarningItemDirective', () => {
  it('should create an instance', () => {
    const directive = new EarningItemDirective();
    expect(directive).toBeTruthy();
  });
});
